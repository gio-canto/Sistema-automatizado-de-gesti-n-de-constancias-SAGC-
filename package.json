{
  "name": "sagc",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "packageManager": "npm@10.8.2",
  "engines": {
    "node": ">=20",
    "npm": ">=10"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "server:install": "npm --prefix server install",
    "server:dev": "npm --prefix server run dev",
    "db:check": "npm --prefix server run db:check",
    "db:bootstrap-admin": "npm --prefix server run db:bootstrap-admin",
    "setup:project": "node scripts/bootstrap-dev.mjs",
    "folio:test": "npm --prefix server run folio:test",
    "chain:test": "npm --prefix server run chain:test",
    "token:test": "npm --prefix server run token:test"
  },
  "dependencies": {
    "@vitejs/plugin-react": "latest",
    "vite": "latest",
    "react": "latest",
    "react-dom": "latest"
  },
  "devDependencies": {}
}
